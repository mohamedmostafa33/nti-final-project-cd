apiVersion: v1
kind: Service
metadata:
  name: reddit-frontend-service
spec:
  selector:
    app: reddit-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
